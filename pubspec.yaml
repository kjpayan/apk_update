name: apk_updater
description: "Un paquete reutilizable para Flutter para la verificación de versiones, descarga de APKs y lanzamiento de instalación nativa en Android."
version: 1.0.0
homepage: https://mobile.managua.gob.ni

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.11.0
  path_provider: ^2.1.5
  open_file: ^3.5.1
  package_info_plus: ^8.2.1
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
